#include "main.h"

/**
 * _abs - checks number
 * @r: entero recibo por main
 * Return: Deveulve r cuando es valor absoluto positivo.
 */

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
