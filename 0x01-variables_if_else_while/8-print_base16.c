#include <stdio.h>

/**
 * main - Lanzador.
 *
 * Return: Always 0.
 */

int main(void)
{
	char a = '0';
	char 1 = 'a';

	for (a = '0'; a <= '9'; a++)
	{
		putchar(a);
	}
	for (1 = 'a'; 1 <= 'f'; 1++)
	{
		putchar(1);
	}
	putchar('\n');
	return (0);
}
